version: 2
jobs:
  build:
    docker:
      - image: circleci/ubuntu-1604:202004-01
    steps:
      - checkout
      - run: make
  #test:
  #  docker:
  #    - image: circleci/<language>:<version TAG>
  #  steps:
  #    - checkout
  #    - run: <command>
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
  #    - test
